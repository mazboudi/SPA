apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: spa-workbench
  namespace: spahost-dev-app
  labels:
    app: spa-workbench
    app.kubernetes.io/name: spa-workbench
    app.kubernetes.io/part-of: software-delivery-automation
spec:
  to:
    kind: Service
    name: spa-workbench
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
