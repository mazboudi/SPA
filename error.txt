I think I may have caught an issue with the workbench. For the PSADT lifecycle section, I set deploy mode to "Silent (default - no UI)", but then I see install_command: 'Invoke-AppDeployToolkit.exe' in package.yaml. Referencing  https://psappdeploytoolkit.com/docs/4.1.x/usage/how-to-deploy#available-parameters, it states that -DeployMode is Interactive by default, so you would need at least install_command: 'Invoke-AppDeployToolkit.exe -DeployMode Silent' to achieve the desired results (I think?). 