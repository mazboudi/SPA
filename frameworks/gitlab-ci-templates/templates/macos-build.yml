##=============================================================================
## macos-build.yml — macOS build CI template (stub)
##=============================================================================
## This template is a no-op stub. macOS titles use pre-packaged .pkg/.dmg
## files placed in macos/src/Files/ — no build step is needed.
##
## This file exists so that older title repos that still include it
## won't fail with a missing template error. It defines no jobs.
##=============================================================================

## No jobs defined — pre-packaged .pkg/.dmg files are used directly.
## The macos_deploy_jamf job (in macos-deploy-jamf.yml) handles everything.
