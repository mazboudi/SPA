# PSADT v4 Lifecycle Configuration
# Generated by SPA New-Title scaffolding on 2026-04-21
# Edit this file to change install/uninstall behavior.
# The pipeline generates Invoke-AppDeployToolkit.ps1 from this at build time.

repair_mode: mirror

pre_install:
  check_disk_space: true
  allow_defer: 3

install:
  actions:
    - type: msi_install
      file_path: "node-v25.8.1-x64.msi"
      arguments: "/QN /norestart"

post_install:
  actions:
    - type: registry_marker

uninstall:
  actions:
    - type: msi_uninstall
      app_name: "Node.js"
      product_code: "{E1584CEF-9AE1-423A-82CF-F1047AA3827E}"

post_uninstall:
  actions:
    - type: remove_registry_marker

