I want to seperate WIndows packaging from MAC packaging.  
Each package will have its own root repository in gitlab. currently we use  
GITLAB_DEFAULT_GROUP=euc/software-package-automation for both.  The new repo:
for Mac will be euc/software-package-automation/mac 
for windows it will be euc/software-package-automation/win

I want to redesign the workbench UI and navigation to use latest Material UI components and a dark mode
- Login page - To be developed later
- Home screen, Seletct Platform 
    - MAC 
    - Windows 
Once a platform is selected, I want to use a collapsable side menu for navigation with the following sections:
- Manage Queue (this will show the pending service requests for selected platfomrm )
- New Package
- Edit Packages
- Settings

depending on the selection on side menu then organize all the stages as menu Action:
Installer, PSADT, Intune, Review & Export for Windows.  
Mac Installer, macOS Config and Review & Export for Mac.  

Leave the tab structure and corresponding functionality under each of the stages 


The setting page should allow me to set all that is in the .env file and also 
set the default windows size (Standard, Wide, Full)
