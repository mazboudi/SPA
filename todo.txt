need to chat about the following: Start-Process is converted into a Run Process card (which generates Start-ADTProcess).
Copy-Item is converted into a Copy File card (which generates Copy-ADTFile).
Remove-Item is converted into a Remove File/Folder card (which generates Remove-ADTFile or Remove-ADTFolder).
New-ItemProperty / Set-ItemProperty are converted into a Set Registry Key card (which generates Set-ADTRegistryKey).
 
the ones in red should not be converted, because technically they can be used for things other than file operations or registry operations. While it's unlikely, it's technically not correct to convert them. For Start-Process, it works a bit differently than Start-ADTProcess. Google AI does a pretty good job explaining this input: "psadt start-adtprocess does not work for certain executables but start-process does"
 
so unless you want to build in all those fixes, don't convert start-process either. Now I understand why Joe was having trouble with it for certain executables. Anyway, he's pretty adamant that we don't convert any native powershell commands. Only convert v3 to v4.
 