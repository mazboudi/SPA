•	NEW TITLE
o	PSADT variables section is empty in builder when I first hit the Visual Action Builder tab. When navigating away and back, variable section is pre-populated as I would expect.
o	For new title, DeployAppScriptVersion locked variable in Builder shows as 4.1.0. Shows correct value in Script Developer. 
o	If user goes back and changes project version (e.g. by applying form from winget), PSADT $appVersion variable is not updated. Apply form did change project version, but Project Version needs to dynamically drive PSADT variables. Basically, anything in the Project Info and Installer sections should dynamically update applicable PSADT builder sections. More testing reveals that builder is not updated if you update anything in Project Info or Installer sections. I tried updating, for example, Install Arguments and builder data remains at previous values.
o	Intune App Name not updating dynamically when project version is updated, but Intune App Version is updated. App Name remains Project Display Name + “old” Project Version.
•	For Visual Action Builder, give option to expand all items in a specific section (e.g. expand all items in Variable Declaration section instead of having to expand them one by one). 
