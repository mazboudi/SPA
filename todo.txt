in the workbench, the paclage ID shoulf always be derived from the title Display name and read-only. that applies to all cases 
such as when creating new, editing, cloning or refactoring.  
When creating a new Title: 
    1 - Always verify the Package ID agaisnt Gitlab and warn if duplucate exist display warning (proceed or Load and edit existing?)
    2 - If Load and edit is selected, don't re-disply warning.
    3 - if proceed selected, proceed with loading the existing project. 

When loading a project from queue:
    1- Lock Package ID for that project to prevent it's editing by other users.
    2- Verify Package Version exists in GitLab
    
When editing an existing title:
    1- Don't show gitlab warning. it redundent

When cloning:
    1- paclage ID shoulf always be derived from the title Display name  and read-only
    2- Verify Package Version exists in GitLab and proceed with warning
    
